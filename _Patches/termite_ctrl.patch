commit 3718467a9f5e216ffc5e2ae8b4b7f7280ab35e01
Author: Narrat <autumn-wind@web.de>
Date:   Wed Dec 13 00:05:47 2017 +0100

    Add ctrl for opening links

diff --git a/trunk/0002_additional_button_for_url.diff b/trunk/0002_additional_button_for_url.diff
new file mode 100644
index 0000000..73ffe51
--- /dev/null
+++ b/trunk/0002_additional_button_for_url.diff
@@ -0,0 +1,12 @@
+--- a/termite.cc
++++ b/termite.cc
+@@ -1173,7 +1173,7 @@
+ }
+ 
+ gboolean button_press_cb(VteTerminal *vte, GdkEventButton *event, const config_info *info) {
+-    if (info->clickable_url && event->type == GDK_BUTTON_PRESS) {
++    if (info->clickable_url && event->type == GDK_BUTTON_PRESS && (event->state & GDK_CONTROL_MASK)) {
+ #if VTE_CHECK_VERSION (0, 49, 1)
+         auto match = make_unique(vte_terminal_hyperlink_check_event(vte, (GdkEvent*)event), g_free);
+         if (!match) {
+
diff --git a/trunk/PKGBUILD b/trunk/PKGBUILD
index da56f84..a839627 100644
--- a/trunk/PKGBUILD
+++ b/trunk/PKGBUILD
@@ -8,8 +8,17 @@ arch=('x86_64')
 url="https://github.com/thestinger/termite/"
 license=('LGPL')
 makedepends=('git' 'vte3-ng' 'ncurses')
-source=("git+https://github.com/thestinger/termite#tag=v$pkgver")
-md5sums=('SKIP')
+source=("git+https://github.com/thestinger/termite#tag=v$pkgver"
+        "0002_additional_button_for_url.diff")
+md5sums=('SKIP'
+         '1c9a2260b44aff0f6a6120be4bb2aa66')
+
+
+prepare() {
+  cd $pkgname
+  # Add additional button for opening links
+  patch -Np1 -i $srcdir/0002_additional_button_for_url.diff
+}
 
 build() {
   cd $pkgbase
