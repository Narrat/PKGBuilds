commit e152086e8ad37b60dc545ff3550624e125376cec
Author: Narrat
Date:   Mon Nov 20 18:56:18 2017 +0100

    ffmpeg: Enable mmal
    
    So mpv can use this to use the GPU on the pi
    
    Changes involved:
     * raspberrypi-firmware-tools to deps (needed for mmal)
     * add libxv
       * caused by not building in a clean chroot

diff --git a/extra/ffmpeg/PKGBUILD b/extra/ffmpeg/PKGBUILD
index 9cb67915b..f2dd70ef0 100644
--- a/extra/ffmpeg/PKGBUILD
+++ b/extra/ffmpeg/PKGBUILD
@@ -19,8 +19,8 @@ license=('GPL3')
 depends=('alsa-lib' 'bzip2' 'fontconfig' 'fribidi' 'glibc' 'gmp' 'gnutls' 'gsm'
          'jack' 'lame' 'libavc1394' 'libiec61883' 'libmodplug' 'libpulse'
          'libraw1394' 'libsoxr' 'libssh' 'libtheora' 'libvdpau' 'libwebp'
-         'libx11' 'libxcb' 'opencore-amr' 'openjpeg2' 'opus' 'sdl2' 'speex'
-         'v4l-utils' 'xz' 'zlib'
+         'libx11' 'libxcb' 'libxv' 'opencore-amr' 'openjpeg2' 'opus' 'sdl2' 'speex'
+         'v4l-utils' 'raspberrypi-firmware-tools' 'xz' 'zlib'
          'libass.so' 'libbluray.so' 'libfreetype.so' 'libva-drm.so' 'libva.so'
          'libva-x11.so' 'libvidstab.so' 'libvorbisenc.so' 'libvorbis.so'
          'libvpx.so' 'libx264.so' 'libx265.so' 'libxvidcore.so')
@@ -44,6 +44,7 @@ prepare() {
   patch -Np1 -i ../fs56089.patch
 }
 
+
 build() {
   cd ffmpeg-${pkgver}
 
@@ -89,6 +90,7 @@ build() {
     --enable-libxvid \
     --enable-shared \
     --enable-version3 \
+    --enable-mmal \
     $CONFIG
 
   make
